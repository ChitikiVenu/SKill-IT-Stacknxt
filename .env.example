# Supabase Auth (kept on the frontend — this is Supabase's own auth service, not the API below)
VITE_SUPABASE_URL=https://bxagqpedroubdzftxcjr.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_DOfRXi6eaM3egcW7x0JHbg_UY8a_dr_

# Skill IT Education Node.js/Express API (see backend/ folder) — handles all data operations
# (lead capture, enrollments) so the frontend never touches Supabase tables directly.
VITE_API_URL=http://localhost:4001

VITE_WHATSAPP_NUMBER=919177715978
